diff --git a/node_modules/gh-pages/lib/git.js b/node_modules/gh-pages/lib/git.js
index d4c5724..948cdc1 100644
--- a/node_modules/gh-pages/lib/git.js
+++ b/node_modules/gh-pages/lib/git.js
@@ -143,7 +143,16 @@ Git.prototype.rm = function (files) {
   if (!Array.isArray(files)) {
     files = [files];
   }
-  return this.exec('rm', '--ignore-unmatch', '-r', '-f', '--', ...files);
+  // Batch files to avoid ENAMETOOLONG on Windows (max ~8191 chars per cmd line)
+  const batchSize = 50;
+  let promise = Promise.resolve(this);
+  for (let i = 0; i < files.length; i += batchSize) {
+    const batch = files.slice(i, i + batchSize);
+    promise = promise.then((git) =>
+      git.exec('rm', '--ignore-unmatch', '-r', '-f', '--', ...batch),
+    );
+  }
+  return promise;
 };
 
 /**
